#include "testApp.h"
#include "stdio.h"

testApp::testApp(){
}

/*
 next:
	output patterns
	sync pattern update to camera return
	compute mesh in real time!
	the image-saving is probably the bottlenck at first...
*/

void testApp::setup(){
	capture.setup(640, 480);
}

void testApp::update(){
}

void testApp::draw(){
}
